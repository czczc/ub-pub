module.exports = {
  // publicPath: process.env.NODE_ENV === 'production' ? '/ub-pub/' : '/',
  publicPath: process.env.NODE_ENV === 'production' ? '/~chao/uboone/publications/' : '/',
  "transpileDependencies": [
    "vuetify"
  ]
}